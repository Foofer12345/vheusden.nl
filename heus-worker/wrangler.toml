name = "heus-hub"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets are NOT set here (that would leak them). Set them with:
#   npx wrangler secret put HEUS_PASSWORD
#   npx wrangler secret put ANTHROPIC_API_KEY
#   ...etc (see README.md)
